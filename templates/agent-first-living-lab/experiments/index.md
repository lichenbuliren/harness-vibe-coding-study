# Experiments / 实验

`experiments/` 记录实验过程中发生的事情。

`experiments/` records what happened during experiments.

请将其与 `evals/` 分开，后者定义如何评判质量。

Keep this separate from `evals/`, which defines how quality is judged.

## Sections / 分区

- `reports/`
- `task-samples/`
