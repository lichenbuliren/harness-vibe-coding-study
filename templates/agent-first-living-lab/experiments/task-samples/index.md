# Task Samples / 任务样本

`experiments/task-samples/` 存储用于评估项目 harness 的样本任务。

`experiments/task-samples/` stores sample tasks for evaluating the project
harness.

随着项目了解到哪些 agent 行为至关重要，逐步添加任务样本。

Add task samples as the project learns which agent behaviors matter.
