# Lab Agent Notes

Lab work should produce evidence.

Before claiming completion, run the relevant checks, verify the user-facing path
when relevant, and record meaningful findings in `harness/runs/`,
`experiments/reports/`, or `docs/evolution/`.
