# {{PROJECT_NAME}}

{{PROJECT_NAME}} is an Agent-First Living Lab.

It is structured so AI agents and humans can understand the project, preserve
the mainline, verify work, and record stage-level learning.

## Current Phase

{{CURRENT_PHASE}}

## How To Read This Repository

- Start with `CONTEXT.md` for durable project context.
- Read `AGENTS.md` for agent operating rules.
- Use `docs/` for methodology, workflows, patterns, standards, and evolution.
- Use `agents/` for roles, playbooks, and handoff formats.
- Use `harness/`, `evals/`, and `experiments/` for evidence and validation.
- Put product source code at the repository root when an app pack is selected.
- Use `decisions/` for concise decision records.
