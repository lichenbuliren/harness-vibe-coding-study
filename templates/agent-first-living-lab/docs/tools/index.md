# Tools

`docs/tools/` stores tool notes, install guidance, and optional skill notes.

Only add a tool here when future agents need a durable reader path.

Use `../../harness/capability-discovery.md` before searching for or
recommending new skills, plugins, tools, playbooks, scripts, or runtime
capabilities.
