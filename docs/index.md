# Documentation Index / 文档索引

`docs/` 存储 harness + vibe coding 研究的方法论和知识。

`docs/` stores methodology and knowledge for the harness + vibe coding study.

## Shareable Summaries / 可分享总结

- `harness-engineering-summary-zh.md`：面向中文读者的 harness 工程总结，涵盖其定义及在实际项目中的实践方式。
  Chinese share-oriented summary of what harness engineering is and how to practice
  it in real projects.

## Sections / 分区

- `principles/`：稳定的原则和约束。
  Stable principles and constraints.
- `workflows/`：可重复的工作流程。
  Repeatable work processes.
- `patterns/`：实践中发现的可复用模式。
  Reusable patterns discovered through practice.
- `standards/`：跨领域项目标准，未来 agent 应将其视为可复用规则。
  Cross-cutting project standards that future agents should treat as reusable
  rules.
- `tools/`：工具说明与安装指导。
  Tool notes and installation guidance.
- `evolution/`：阶段级别的叙事记录和可分享的经验总结。
  Stage-level narrative records and shareable lessons.
- `superpowers/`：通过 Superpowers 工作流生成的规约和计划。
  Specs and plans created through the Superpowers workflow.

## Reuse Surfaces / 复用界面

- `../templates/`：在阶段 3C 中提取的可复用骨架。
  Reusable skeletons extracted during Phase 3C.

## Placement Rule / 放置规则

- 将持久信念放在 `principles/`。
  Put durable beliefs in `principles/`.
- 将分步流程放在 `workflows/`。
  Put step-by-step procedures in `workflows/`.
- 将可复用方案放在 `patterns/`。
  Put reusable solutions in `patterns/`.
- 将跨领域强制性规则放在 `standards/`。
  Put cross-cutting mandatory rules in `standards/`.
- 将工具相关说明放在 `tools/`。
  Put tool-specific notes in `tools/`.
- 将阶段级回顾和可分享成果放在 `evolution/`。
  Put stage-level retrospection and shareable outcomes in `evolution/`.
