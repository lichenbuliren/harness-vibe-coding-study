# Patterns

This directory stores reusable patterns discovered through practice.

Use it when a workflow, directory shape, prompt structure, evaluation approach,
or handoff format has been validated enough to reuse.

## Current Patterns

- `standard-capture-loop.md`: high-priority methodology for turning meaningful
  user corrections, failed runs, review findings, and repeated friction into
  discoverable project standards.
