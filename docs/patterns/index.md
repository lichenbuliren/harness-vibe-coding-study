# Patterns / 模式

本目录存储通过实践发现的可复用模式。

This directory stores reusable patterns discovered through practice.

当一个工作流、目录结构、提示结构、评估方法或交接格式经过充分验证可以复用时，在此记录。

Use it when a workflow, directory shape, prompt structure, evaluation approach,
or handoff format has been validated enough to reuse.

## Current Patterns / 当前模式

- `standard-capture-loop.md`：高优先级方法论，将有意义的用户纠正、失败运行、评审发现和反复摩擦转化为可发现的项目标准。
  High-priority methodology for turning meaningful user corrections, failed
  runs, review findings, and repeated friction into discoverable project
  standards.
