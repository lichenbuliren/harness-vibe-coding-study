# Keep AGENTS.md as the root operating contract

`AGENTS.md` should stay short enough to be the agent's startup contract: how to
start, stay in scope, verify work, and leave the repository restartable. Longer
generated OMX/runtime material remains useful, but it belongs in workflow
reference documentation so the root contract stays readable and project-owned.
